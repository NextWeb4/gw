# 交付验证矩阵

验证日期：2026-08-01。状态只代表当前工作区和本机环境，不代表已完成生产合规或目标系统兼容认证；标明 Release 的行仍只描述对应已发布版本。

| 交付项 | 验证方式 | 当前状态 | 证据 / 限制 |
| --- | --- | --- | --- |
| v0.7.9 当前可见记录连续浏览 | 第一性原理/开源冲突审计、失败优先纯函数/UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 Python Playwright 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 本机已通过，发布待核验 | 2026-08-01：`kb_router` 只误命中验证码和内存修改等无关材料，按协议读取后排除；两条独立审计均推荐当前可见记录上一条/下一条，而不是保存视图、Kanban、批量操作或自定义字段。最终使用无依赖纯函数从六个 `filtered*` active 数组派生位置和邻居，详情按钮只调用原 `selectBusinessRecord`。失败优先 Vitest、UI 合约和 Chromium 分别先因缺少模块、接线和 DOM 失败；实现后聚焦纯函数 4/4、UI 合约 14/14、桌面/移动 Playwright 2/2 通过，覆盖排序步进、单条边界、六类接线、MRU、键盘、零动作请求、移动 44px 和底栏边界。独立 Python Playwright 验证桌面 360px 与移动 366px 详情栏内标题/状态/导航/操作无重叠，移动按钮 44×44px，两视口无请求、错误或横向溢出；截图位于 `test-results/visible-record-navigation-0.7.9/`。`pnpm lint`、格式检查、131 项单元/合约、主 E2E 79 通过/7 条件跳过、互联网 7/7、内网 3/3 及三类 Web 构建全部通过。首轮主 E2E 的 2 个版本失败来自复用仍为 v0.7.8 的旧 preview，失败快照确认后停服重建，聚焦 2/2 与完整套件均通过。八份 manifest 均为 `0.7.9`；公开构建 25 文件/10 maps，互联网和内网各 15 文件/0 maps，三套各命中 0.7.9 十次且旧版本/秘密模式为 0。Pages 与 Release 仍待推送标签后核验。 |
| v0.7.8 会话级最近访问记录 | 第一性原理/开源冲突审计、失败优先纯函数/UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 Playwright 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-08-01：`kb_router` 未找到适用的 UI 条目；此前无关 APK 加密材料已读取并排除。`http_probe` 复核 VS Code Quick Access、GitHub Command Palette、cmdk 与 kbar 的官方来源和许可证。最终采用无依赖会话 MRU：只存 `{tab,id}`，显式访问更新、去重移首、上限 6、active 变化主动裁剪、恢复不自动复活、刷新清空；空查询显示，输入后整组卸载，选择仍复用原 `openBusinessRecord`。失败优先单测先因模块/裁剪函数不存在而失败，UI 合约先因缺少组和状态边界失败；实现后最近记录单测 3/3、UI 合约 13/13、总计 126 项单元/契约、主 E2E 77 通过/7 条件跳过、互联网 7/7、内网 3/3、lint、格式检查及三类 Web 构建全部通过。E2E 覆盖 A→B→A、搜索无重复、删除/恢复、再次打开、刷新、零请求、移动 44px、无横向溢出和底栏边界。本机 Chromium 的桌面/移动最近项约 56.6/61.9px，移动面板底边约 742.5px、固定底栏顶边 770px，两视口无请求/错误/溢出；八份清单均为 `0.7.8`，公开构建 25 文件/10 maps，互联网和内网各 15 文件/0 maps，三套秘密扫描为 0。[Pages run 30685858239](https://github.com/NextWeb4/gw/actions/runs/30685858239) 与 [Desktop run 30685866201](https://github.com/NextWeb4/gw/actions/runs/30685866201) 成功；线上 HTTP 200 并引用已验证的内容哈希入口 bundle，桌面/移动真实 Chromium 显示页脚 `0.7.8`、正确 MRU、零动作请求/错误/溢出及移动底栏安全边界。[Release v0.7.8](https://github.com/NextWeb4/gw/releases/tag/v0.7.8) 的 12 个非空安装包和校验文件共 13 个地址均 HTTP 200，12 条清单记录格式有效、哈希/文件名唯一且精确覆盖资产。 |
| v0.7.7 全局查找与六类快速新建 | 第一性原理/开源冲突审计、失败优先 UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 Playwright 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-08-01：`kb_router` 只误命中 APK 加密等无关文档，已按协议读取并排除；`http_probe` 复核 cmdk 1.1.1、kbar 0.1.0-beta.47 与 Radix Dialog 官方来源。方案继续复用现有 cmdk，只新增六个稳定 action 与既有编辑器接线，并补齐原抽屉首字段焦点、Tab 循环和关闭焦点恢复。失败优先 UI 合约先在缺少 `create` 类型处失败；最终 lint、格式检查、123 项单元/契约、主 E2E 75 通过/7 条件跳过、互联网 7/7、内网 3/3及三类 Web 构建全部通过。覆盖六类命令、同义词、ArrowDown/Enter/Escape、首字段焦点、Tab 圈闭、普通关闭与动作关闭焦点时序、原未保存守卫、重载后取消零写入、编辑/快速记录/AI 模态互斥、动作期零请求和移动端边界。首次完整主 E2E 因复用短生命周期 preview 导致 75 条 `ERR_CONNECTION_REFUSED`，改用 `CI=1` 独占服务后只发现旧 placeholder 测试定位；改为 dialog/combobox 后最终 75/75 有效用例通过。真实 Chromium 1440×900/390×844 下命令面板为 720×587px/约 369×601px，移动底边约 610px、底栏顶边 770px，命令项最小约 61px，`body.scrollWidth` 等于视口宽度，无错误或请求；截图为 `test-results/command-palette-v077-desktop-1440x900.png` 和 `test-results/command-palette-v077-mobile-390x844.png`。公开构建 25 文件/10 maps，互联网和内网各 15 文件/0 maps；三套各命中 `0.7.7` 10 次，旧版本、用户六位密码、私钥和长格式 API Key 均为 0。[Pages run 30683208784](https://github.com/NextWeb4/gw/actions/runs/30683208784) 与 [Desktop run 30683218107](https://github.com/NextWeb4/gw/actions/runs/30683218107) 成功；线上入口 HTTP 200 并引用已验证的内容哈希入口 bundle。线上 Chromium 桌面/移动均完成六动作、键盘选择、首字段聚焦和关闭回焦，无请求、错误、溢出或底栏遮挡；截图为 `test-results/command-palette-v077-live-desktop-1440x900.png` 和 `test-results/command-palette-v077-live-mobile-390x844.png`。[Release v0.7.7](https://github.com/NextWeb4/gw/releases/tag/v0.7.7) 的 12 个安装包和校验文件共 13 个地址均 HTTP 200 且非空，12 条校验记录格式有效、SHA-256 与文件名分别唯一并完整覆盖安装资产。 |
| v0.7.6 六类业务复制相似记录 | 第一性原理/开源冲突审计、失败优先领域单测、UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 Python Playwright 视觉/几何检查、Pages/Release 线上核验 | 已通过并发布 | 2026-07-31：复制不变量锁定为 active 输入、新 ID/时间戳、生命周期/迁移字段剥离、源对象和嵌套数组不变、主附件 ID 共享、任务/文件/外出结果状态按类型重置、只打开原编辑抽屉且立即未保存。审计 Vikunja TaskDuplicate（AGPL-3.0-or-later）、Web `structuredClone`、lodash.clonedeep 4.5.0 和 rfdc 1.4.1；最终零新增依赖，只借鉴逐字段白名单和新身份语义。领域/契约共 123 项通过；主 E2E 80 条中 73 通过、7 条条件跳过；互联网 7/7、内网 3/3。主 E2E 覆盖六类详情入口、取消零写入、任务保存后原记录保留/新记录选中；跨午夜日历用例按目标月份切换，移动快速记录按钮因浏览器亚像素边界将最小高度提高到 46px。`pnpm lint`、`pnpm format:check`、`pnpm test`、`pnpm build`、`pnpm build:web:internet`、`pnpm build:web:intranet` 均通过。Python Playwright 本机和线上真实 Chromium 均通过桌面复制保存与移动复制取消；移动按钮 44px，`body.scrollWidth` 1440/390，无控制台错误和外部请求；线上截图为 `test-results/copy-record-live-desktop-1440x900.png` 与 `test-results/copy-record-live-mobile-390x844.png`。[Pages run 30648940120](https://github.com/NextWeb4/gw/actions/runs/30648940120) 成功；线上 bundle 中 `0.7.6` 命中 10 次，`0.7.5`/`0.7.4`/`0.3.1` 为 0，并包含“复制相似记录”和未保存提示。[Desktop run 30649026582](https://github.com/NextWeb4/gw/actions/runs/30649026582) 成功；[Release v0.7.6](https://github.com/NextWeb4/gw/releases/tag/v0.7.6) 包含 12 个安装包和 `SHA256SUMS.txt`，13 个地址均 HTTP 200 且非空，12 条校验记录有效、唯一并完整覆盖安装资产。未新增数据库 schema、服务端路由、同步协议、附件格式、持久化秘密或自动联网。 |
| v0.7.5 六类业务回收站与同步墓碑 | 开源/冲突审计、失败优先领域/快照/同步/UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 Python Playwright 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-07-31：审计 Vikunja、Plane 和 AppFlowy 的 AGPL 生命周期设计，只借鉴“软删除、恢复、永久删除、关联清理”，不复制源码或引入依赖；保存视图与现有会话级规则冲突，复制记录不解决误删和多设备复活，均未采用。失败优先测试锁定 active/trash/purged 分区、源对象不变、恢复完整字段/附件、四字段墓碑、快照正文移除、墓碑同步、远端 purge 附件清理和本地 UI 边界。实现后 lint、格式检查、120 项单元/契约、主 E2E 71 项通过/7 项条件跳过、互联网 7 项、内网 3 项及三类 Web 构建通过。覆盖六类软删除、搜索/类型筛选、恢复、永久删除、清空、回收站附件下载、恢复后附件可用、共享附件保留、全局搜索排除、有效 master 附件计算、远端较新墓碑胜出和零外联。内网停止 E2E 首次并行因旧 450ms 定时门在资源压力下提前完成而超时，替换为 Promise 闸门后 3/3 通过。1440×900 与 390×844 Python Playwright 验证无横向溢出、移动内容不遮挡底栏、控件至少 44px、活动回收站图标可见；截图为 `test-results/recycle-desktop-1440x900.png` 和 `test-results/recycle-mobile-390x844.png`。八份清单为 `0.7.5`；公开构建 25 文件/10 source map，互联网和内网各 15 文件/0 source map；三类构建不含 `0.7.4`、`0.3.1`、用户密码、私钥或长格式 API Key。[Pages run 30641433505](https://github.com/NextWeb4/gw/actions/runs/30641433505) 成功；线上 HTTP 200，bundle 中 `0.7.5` 命中 10 次、旧版本为 0，真实 Chromium 完成软删除与回收站打开，`body.scrollWidth=1440`，无错误/外联，截图为 `test-results/recycle-live-v075.png`。[Desktop run 30641434968](https://github.com/NextWeb4/gw/actions/runs/30641434968) 18/18 成功；[Release v0.7.5](https://github.com/NextWeb4/gw/releases/tag/v0.7.5) 的 12 个安装包和校验文件共 13 个地址均 HTTP 200 且非空，12 条校验记录有效、唯一并与安装资产完全一致。 |
| v0.7.4 全局快速任务记录 | 开源/冲突审计、失败优先领域/UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 Python Playwright 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-07-31：审计 Super Productivity（MIT）、Vikunja（AGPL-3.0-or-later）与 Google Tasks 官方交互后，采用 React 19、原生 `<dialog>`、既有确定性提取器和原 `TaskEditor`，不新增依赖。失败优先测试先证明共享字段应用函数与快速记录组件不存在；实现后 lint、格式检查、110 项单元/契约、主 E2E 69 项通过/7 项条件跳过、互联网 7 项、内网 3 项和三类 Web 构建全部通过。覆盖顶栏/工作台/`Shift+A`、输入控件保护、焦点恢复、即时预览、无匹配继续、原文字保留、预填即脏、原保存按钮、模态冲突、零请求和移动 44px。聚焦 E2E 首轮因测试在搜索框留下快捷键输入字符而隐藏新记录，清空测试筛选后 2/2 与完整套件通过。1440×900 对话框 710×490px；390×844 对话框 370×740px、底边 750px，固定底栏顶边 770px，`body.scrollWidth` 分别为 1440/390；无控制台错误或外部请求。截图为 `artifacts/quick-capture-v074-1440x900.png` 与 `artifacts/quick-capture-v074-390x844.png`。八份清单为 `0.7.4`，互联网/内网 source map 为 0，三类构建不含旧版本、用户密码、测试秘密、私钥或长格式 API Key。[Pages run 30633056243](https://github.com/NextWeb4/gw/actions/runs/30633056243) 成功；线上 HTTP 200，真实 Chromium 显示页脚 `0.7.4`、快速记录和确定性预览，对话框 710×490px、`body.scrollWidth=1440`，无错误/外联，截图为 `artifacts/quick-capture-v074-live.png`。[Desktop run 30633059732](https://github.com/NextWeb4/gw/actions/runs/30633059732) 18/18 成功；[Release v0.7.4](https://github.com/NextWeb4/gw/releases/tag/v0.7.4) 的 12 个安装包和校验文件共 13 个地址均为 HTTP 200，12 条校验记录有效、唯一且与安装资产完全一致。 |
| v0.7.3 跨模块工作焦点 | 开源/冲突审计、失败优先单元测试、UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-07-31：完成 Super Productivity、Nextcloud Tasks、Vikunja 和 Google Tasks / Calendar 的官方来源、许可证、维护状态与架构冲突审计，采用零依赖的本地纯函数与工作台轻量视图。首个单元回归因 `work-overview.ts` 不存在而按预期失败；实现后 lint、格式检查、108 项单元/契约测试、主 E2E 67 项通过/7 项条件跳过、互联网 7 项、内网 3 项和三类 Web 构建均通过。覆盖逾期只属于未完成任务、过去非任务排除、今天六类事项、未来 7 天边界、第 8 天排除、无效/空日期未排期、已完成任务排除、稳定排序、范围切换、既有详情跳转和零外联。完整 E2E 首轮唯一失败是“打开事务日历”与导航名称模糊匹配，改为“查看完整日历”后聚焦 4/4 与完整套件重跑通过。1440×900 下 `body.scrollWidth=1440`、主区高 342px、概览顶边 604px；390×844 下 `body.scrollWidth=390`、范围按钮 72px、记录行 68px、概览滚动后底边 555px、主区底边 760px、固定底栏顶边 770px；两视口无控制台错误或外部请求。截图为 `artifacts/work-overview-v073-1440x900.png`、`artifacts/work-overview-v073-390x844-top.png` 和 `artifacts/work-overview-v073-390x844.png`。八份清单版本均为 `0.7.3`，互联网/内网 source map 为 0，三类构建中旧版本、用户给定密码、E2E 秘密标记、私钥和长格式 API Key 均为 0 命中。[Pages run 30628737861](https://github.com/NextWeb4/gw/actions/runs/30628737861) 成功；线上 bundle 中 `0.7.3` 命中 10 次、`0.7.2` 为 0，真实 Chromium 显示三个范围和页脚 `0.7.3`，可进入原任务详情，`body.scrollWidth=1440`，无控制台错误或外部请求，截图为 `artifacts/work-overview-v073-live.png`。[Desktop run 30628740473](https://github.com/NextWeb4/gw/actions/runs/30628740473) 的 verify、4 个 Linux、4 个 Windows、8 个 Debian 10/12 启动门和 release 全部成功；[v0.7.3 Release](https://github.com/NextWeb4/gw/releases/tag/v0.7.3) 的 12 个安装包与校验清单共 13 个地址均为 HTTP 200，校验文件含 12 条有效唯一记录。 |
| v0.7.2 六类台账统一事务日历 | 开源/冲突审计、失败优先单元/UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-07-31：已完成第一性原理和五个候选方案审计，确定采用零依赖的本地纯函数与独立日历视图；单元/UI 合约首轮分别因 `agenda.ts` / `AgendaView.tsx` 不存在而按预期失败。实现后 lint、格式检查、105 项单元/契约、主 E2E 65 项通过/7 项条件跳过、互联网 7 项、内网 3 项及三类 Web 构建均通过；覆盖固定六字段映射、无效日期排除、周一起始 42 日网格、前后月、今天、六类筛选、同日顺序、16 个全局查找导航项、原详情链路和零外联。1440×900 与 390×844 的 `body.scrollWidth` 分别为 1440 和 390；桌面月历/议程宽 798/303px，移动日期按钮最小 44px、议程按钮最小 64px、月历底边 747px 位于固定底栏 770px 之上，滚动后的议程底边 662px 不受底栏遮挡；无控制台错误或外部请求。截图为 `artifacts/agenda-v072-1440x900.png`、`artifacts/agenda-v072-390x844.png` 和 `artifacts/agenda-v072-390x844-selected-day.png`。八份清单版本均为 `0.7.2`，互联网/内网 source map 为 0，构建中旧版本与秘密扫描为 0 命中。[Pages run 30623449945](https://github.com/NextWeb4/gw/actions/runs/30623449945) 成功；线上真实 Chromium 显示 `0.7.2`、42 日网格和原详情跳转，HTTP 200、无错误/外联，截图为 `artifacts/agenda-v072-live.png`。[Desktop run 30623446895](https://github.com/NextWeb4/gw/actions/runs/30623446895) 18/18 任务成功；[Release v0.7.2](https://github.com/NextWeb4/gw/releases/tag/v0.7.2) 的 12 个安装包和 `SHA256SUMS.txt` 共 13 个地址均为 HTTP 200，校验文件含 12 条有效唯一记录。 |
| v0.7.1 六类台账统一本地筛选与排序 | 开源/冲突审计、失败优先单元/UI 合约、主/互联网/内网 E2E、lint、格式检查、三类 Web 构建、1440×900 与 390×844 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-07-31：已完成第一性原理与许可证审计，确认不引入 TanStack Table 或 AGPL 源码；首轮单元/UI 合约先因 `ledger-view.ts` 不存在而失败。实现后 lint、格式检查、101 项单元/契约、主 E2E 63 项通过/7 项条件跳过、互联网 7 项、内网 3 项及三类 Web 构建均通过；覆盖组合查询/筛选/排序、源数组不变、空日期顺序、动态选项去重、跨模块会话恢复、六模块筛选、清除和物资全量库存。完整 E2E 首轮发现清除按钮的无障碍名称与“任务管理”导航模糊匹配，最小改名后聚焦 4/4 和完整套件重跑通过。1440×900 与 390×844 的 `body.scrollWidth` 分别为 1440 和 390；桌面详情栏 360px，移动筛选/排序/清除均为 44px，固定底栏顶边 770px，无控制台错误或外部请求。截图为 `artifacts/ledger-view-v071-1440x900.png` 与 `artifacts/ledger-view-v071-390x844.png`。八份清单版本均为 `0.7.1`，互联网/内网 source map 为 0，源码和构建秘密扫描为 0 命中。[Pages run 30616709394](https://github.com/NextWeb4/gw/actions/runs/30616709394) 已成功并实测线上页脚 `0.7.1`、视图控制可用、无外部请求。桌面首次 [run 30616712071](https://github.com/NextWeb4/gw/actions/runs/30616712071) 仅因 Docker Hub 拉取超时导致 Debian 12 arm64 冒烟 exit 125；重推同一标签后的 [run 30617653755](https://github.com/NextWeb4/gw/actions/runs/30617653755) 全部成功。Release 的 12 个安装包和 `SHA256SUMS.txt` 共 13 个地址均返回 HTTP 200，校验文件含 12 条有效唯一记录。 |
| v0.7.0 本机全局查找 | 开源/冲突审计、失败优先 UI 合约、主/互联网/内网 E2E、lint、格式检查、单元/契约、三类 Web 构建、1440×900 与 390×844 Python Playwright 视觉/几何检查、安全扫描、Pages/Release 线上核验 | 已通过并发布 | 2026-07-31：首个 UI 合约先因 `GlobalSearch.tsx` 不存在而失败，锁定 cmdk、键盘、既有导航链路、模态冲突和 44px 触控边界。实现后 95 项单元/契约测试、主 E2E 61 项通过/7 项条件跳过、互联网 7 项、内网 3 项均通过；覆盖十五个导航项、六类业务分组、`Ctrl/Cmd+K`、上下键/Enter/Escape、焦点恢复、空状态、备注秘密标记不进入索引、编辑抽屉与 AI overlay 禁止模态叠加，以及全流程零第三方请求。视觉检查首次发现 cmdk `className` 只落在内部 root、portal Dialog 未取得预期宽度，改用 `contentClassName` 后重新通过 UI 合约、双视口聚焦 E2E 和 Web 构建。1440×900 与 390×844 的 `body.scrollWidth` 分别为 1440 和 390，对话框分别为 720×303px 和 370×283px，移动入口 44×44px，固定底栏顶边 770px、对话框底边 293px；无控制台错误或外部请求。截图为 `artifacts/global-search-v070-1440x900.png` 与 `artifacts/global-search-v070-390x844.png`。公开构建含 10 处 `0.7.0` 且不含 `0.6.9`/`0.3.1`，互联网/内网 source map 均为 0，构建产物不含用户给定六位密码、E2E 秘密标记或会话 Key。新增 `cmdk@1.1.1`（MIT），未新增 schema、私有 API、后台索引、遥测或联网行为。[Pages run 30611903426](https://github.com/NextWeb4/gw/actions/runs/30611903426) 与 [desktop run 30611903306](https://github.com/NextWeb4/gw/actions/runs/30611903306) 均成功；线上真实 Chromium 显示页脚 `0.7.0`，全局查找可进入原台账详情，且无外部请求。Release 的 13 个下载地址均为 HTTP 200，校验文件含 12 条有效唯一记录。 |
| v0.6.9 业务抽屉未保存保护 | lint、格式检查、单元/契约、主/互联网/内网 E2E、构建、桌面/390×844 Playwright、发布核验 | 已通过并发布 | 2026-07-31：先失败测试证明六类编辑抽屉直接关闭会静默丢弃字段和暂存附件。最小实现后，共享 `useUnsavedChangesGuard` 覆盖取消、关闭按钮、外侧、Escape 与 `beforeunload`；附件读取开始即进入脏状态并禁用保存，确认放弃递增会话代次，延迟 SHA-256 完成后不会重开或改写已关闭编辑器。本机 lint、格式检查、94 项单元/契约测试、主 E2E 59 项通过/7 项条件跳过、互联网 7 项、内网 3 项和生产构建均通过。初次并行运行三套 E2E 时内网套件受本机资源竞争超时，三套分别独立重跑后全部通过。1440×900 与 390×844 的 `body.scrollWidth` 分别为 1440 和 390，抽屉右边界分别为 1440 和 390，移动关闭按钮 44×44px，检查过程无第三方请求；截图为 `artifacts/unsaved-drawer-v069-1440x900.png` 与 `artifacts/unsaved-drawer-v069-390x844.png`。私有构建 source map 均为 0；[Pages run 30607878047](https://github.com/NextWeb4/gw/actions/runs/30607878047) 与 [desktop run 30607877902](https://github.com/NextWeb4/gw/actions/runs/30607877902) 均成功，线上 bundle 仅包含 `0.6.9` 并含未保存/附件处理中反馈，[Release v0.6.9](https://github.com/NextWeb4/gw/releases/tag/v0.6.9) 的 13 个下载地址均为 HTTP 200，校验文件含 12 条有效唯一记录。未新增依赖、数据库、服务端协议、持久化秘密或联网行为。 |
| v0.6.8 AI 生成请求生命周期 | sync-client 生成取消单测、互联网重复/停止/桌面迟到 E2E、内网停止 E2E、三套完整 E2E、lint、格式检查、构建、1440×900 与 390×844 视觉/几何检查、Pages/Release 线上核验 | 已通过并发布 | 2026-07-30：先失败测试证明 `DirectAiClient`、`RelayAiClient`、`PrivateSyncClient` 的生成方法忽略调用方 `AbortSignal`，互联网与桌面 UI 也没有生成忙碌态和停止入口。最小实现后，客户端 lint、格式检查、93 项单元/契约测试、主 E2E 57 项通过/7 项条件跳过、互联网 7 项、内网 3 项和生产构建均通过。1440×900 与 390×844 检查的 `body.scrollWidth` 分别为 1440 和 390；移动端生成/停止按钮均为 44px，高频停止操作不再被旧 toast 遮挡，浏览器、内网与桌面迟到响应均未写入结果或重复历史。截图保存在工作区根目录 `artifacts/ai-generation-v068-1440x900.png` 与 `artifacts/ai-generation-v068-390x844.png`，不进入发布产物。[Pages run 30558339464](https://github.com/NextWeb4/gw/actions/runs/30558339464) 与 [desktop run 30558345545](https://github.com/NextWeb4/gw/actions/runs/30558345545) 均成功；线上 bundle 仅包含 `0.6.8` 并含生成忙碌/停止文案，Release 含 12 个安装包和 `SHA256SUMS.txt`，13 个下载地址均返回 HTTP 200，校验文件含 12 条有效记录。未新增依赖、服务端协议、数据库、秘密持久化、后台刷新或自动联网；真实第三方上游未验证。 |
| v0.6.7 模型刷新与状态保留 | sync-client/desktop 单测、互联网乱序/取消 E2E、三套完整 E2E、lint、格式检查、构建、1440×900 与 390×844 视觉/几何检查、Pages/Release 线上核验 | 已通过并发布 | 2026-07-30：先失败测试证明旧实现会排序模型、忽略调用方 AbortSignal，并在 E2E 中默认选择错误顺序。实现后客户端 lint、格式检查、90 项单元/契约测试、主 E2E 57 项通过/7 项条件跳过、互联网 5 项、内网 2 项和生产构建均通过。视觉检查中 `body.scrollWidth` 分别为 1440 和 390；390px 下刷新/停止按钮均高 44px，刷新中仍保留 15 项目录、筛选词和长模型 ID，停止后迟到结果未覆盖当前状态。本机截图保存在工作区根目录 `artifacts/model-refresh-v067-1440x900.png` 与 `artifacts/model-refresh-v067-390x844.png`，不进入发布产物。[Pages run 30546950074](https://github.com/NextWeb4/gw/actions/runs/30546950074) 与 [desktop run 30547010378](https://github.com/NextWeb4/gw/actions/runs/30547010378) 均成功；线上 bundle 仅包含 `0.6.7`，Release 含 12 个安装包和 `SHA256SUMS.txt`，13 个下载地址均返回 HTTP 200，校验文件含 12 条有效记录。未新增依赖、持久化、后台刷新、服务端协议或数据库变更；真实第三方上游未验证。 |
| v0.6.6 模型目录筛选与迟到响应防护 | `pnpm lint`、`pnpm format:check`、`pnpm test`、三套 E2E、`pnpm build`；1440×900 与 390×844 Python Playwright 视觉/几何检查；Pages/Release 线上核验 | 已通过并发布 | 2026-07-30：乱序网络 E2E 覆盖慢旧地址与快新地址、忙碌态、15 项模型筛选、无匹配时保留当前模型、清除筛选和长 ID 不扩宽页面。客户端 lint、格式检查、87 项单元/契约测试、主 E2E 57 项通过/7 项条件跳过、互联网 4 项、内网 2 项和生产构建均通过。1440×900 与 390×844 的 `body.scrollWidth` 分别等于 1440 和 390；移动端筛选输入 16px、清除按钮 44×44px、选择器 44px，焦点环可见且长模型 ID 可换行查看。本机截图保存在工作区根目录的 `artifacts/model-catalog-v066-1440x900.png` 与 `artifacts/model-catalog-v066-390x844.png`，不进入发布产物。[Pages run 30542197525](https://github.com/NextWeb4/gw/actions/runs/30542197525) 与 [desktop run 30542253012](https://github.com/NextWeb4/gw/actions/runs/30542253012) 均成功；线上 bundle 仅包含 `0.6.6`，Release 含 12 个安装包和 `SHA256SUMS.txt`，13 个下载地址均返回 HTTP 200，校验文件含 12 条有效记录。未新增依赖、协议字段、持久化或自动联网入口；服务端保持 `0.2.3`，真实第三方上游未验证。 |
| v0.6.5 本机中转连接诊断 | 服务端 `pnpm lint`、`pnpm format:check`、`pnpm test:integration`、`pnpm build`；客户端 `pnpm lint`、`pnpm format:check`、`pnpm test`、三套 E2E、`pnpm build`；管理页 1440×900 与 390×844 Python Playwright 检查；Pages/Release 线上核验 | 已通过并发布 | 2026-07-30：隔离测试覆盖 Bearer 401 到 `x-api-key`、`/v1/models` 404 到 Base URL `/models`、无效 JSON、空模型、网络失败、未启用 provider 管理测试、未配置站点拒绝、诊断不改 revision、公开目录仍隐藏停用站点，以及诊断响应不含真实 URL、host、Key 或底层异常。服务端 34 项通过、2 项 PostgreSQL 条件测试跳过；客户端 86 项单元/契约测试、主 E2E 57 项通过/7 项条件跳过、互联网 3 项、内网 2 项通过。管理页显式测试、模型预览、编辑清除旧结果和迟到结果防护在两视口均无页面级横向溢出。[Pages run 30530132922](https://github.com/NextWeb4/gw/actions/runs/30530132922) 与 [desktop run 30530253737](https://github.com/NextWeb4/gw/actions/runs/30530253737) 均成功；线上入口 bundle 包含 `0.6.5`，不含 `0.6.4` 或 `0.3.1`；Release 含 12 个安装包和 `SHA256SUMS.txt`，校验文件含 12 条有效记录。未新增依赖、数据库 schema、配置文档版本、客户端诊断协议或自动联网；真实 PostgreSQL 和真实第三方上游未在本机验证。 |
| v0.6.4 中转站双路径模型发现 | 服务端 `pnpm lint`、`pnpm format:check`、`pnpm test:integration`、`pnpm build`；客户端 `pnpm lint`、`pnpm format:check`、`pnpm test`、三套 E2E、`pnpm build`；本机管理页 1440×900 与 390×844 Playwright 检查 | 已通过并发布 | 2026-07-29：隔离测试覆盖 `/api/v1/models` 到 `/api/models`、生成 404 路径切换、生成成功但无效 JSON 不重试、Bearer/`x-api-key`、模型顺序和大小写去重、旧配置默认 `auto`，并确认公开响应不含 `apiPath`、真实地址或 Key。服务端 30 项通过、2 项 PostgreSQL 条件测试跳过；客户端主 E2E 57 项通过/7 项条件跳过，互联网 3 项、内网 2 项通过。管理页三站点均显示“自动兼容 / 自动补 `/v1` / 直接追加资源”，1440×900 与 390×844 无页面级横向溢出。[服务端 run 30468673475](https://github.com/NextWeb4/hxhwang-gw-server/actions/runs/30468673475)、[Pages run 30468731438](https://github.com/NextWeb4/gw/actions/runs/30468731438) 和 [desktop run 30468732985](https://github.com/NextWeb4/gw/actions/runs/30468732985) 均成功；线上 bundle 仅包含 `0.6.4`，Release 含 12 个安装包与 `SHA256SUMS.txt`，12 条校验记录格式有效。未新增依赖、数据库 schema、自动联网入口或客户端秘密字段；真实第三方站点因本机没有已启用 provider 尚未实测。 |
| v0.6.2 窄屏详情与 AI 长页操作优化 | `pnpm lint`、`pnpm format:check`、`pnpm test`、三套 E2E、`pnpm build`、1440×900 与 390×844 真实浏览器截图/几何检查 | 已通过本机回归 | 2026-07-28：主 E2E 57 项通过、7 项条件跳过；互联网 3 项、内网 2 项通过。覆盖窄屏选中记录后详情进入视口和返回列表、桌面返回按钮隐藏、AI 三分区跳转、四步状态、模型/内网连接就绪后配置自动收起且可展开。390px 实测 `body.scrollWidth=390`，详情定位后 `.main-area.scrollTop=590`、返回后为 65，列表操作按钮 44×44px；1440px 三栏仍并排。无新依赖、许可证、schema、服务端协议或自动联网变化。发布状态以 `v0.6.2` Actions 为准。 |
| v0.6.3 中转站模型鉴权兼容 | 服务端 `pnpm lint`、`pnpm format:check`、`pnpm test:integration`、`pnpm build`；客户端 `pnpm lint`、`pnpm format:check`、`pnpm test`、三套 E2E、`pnpm build`；本机管理页 1440×900 与 390×844 Playwright 检查 | 已通过并发布 | 2026-07-28：隔离测试覆盖 `/v1/models` 路径、Bearer 401 后 `x-api-key` 回退、显式 `x-api-key`、模型解析和旧配置默认 `auto`；服务端 25 项通过/2 项 PostgreSQL 条件跳过，主 E2E 57 项通过/7 项条件跳过，互联网 3 项、内网 2 项通过。管理页三站点均显示“自动 / Bearer / x-api-key”，桌面与移动无页面级横向溢出。[Pages run 30356986954](https://github.com/NextWeb4/gw/actions/runs/30356986954) 和 [desktop run 30356989688](https://github.com/NextWeb4/gw/actions/runs/30356989688) 均成功；线上显示 0.6.3，Release 含 12 个分版/平台/架构安装资产与 `SHA256SUMS.txt`。未新增依赖、数据库 schema 或客户端秘密字段。 |
| v0.6.1 Chrome 回环权限修复 | 同步客户端单测、互联网 E2E、Chrome 151 从 Pages 到真实 `127.0.0.1:8787` 的拒绝/授予对照 | 已通过并发布 | 未授权时 Pages 显示网站设置恢复提示且不暴露原始 `Failed to fetch`；授予 `local-network-access` 后真实密码会话与 provider 目录均为 200。请求标记 `targetAddressSpace: 'loopback'`，不使用产品绕过参数。[Pages run 30338701480](https://github.com/NextWeb4/gw/actions/runs/30338701480) 成功并实测页脚 0.6.1；[desktop run 30338765244](https://github.com/NextWeb4/gw/actions/runs/30338765244) 的 8 个分版构建、8 个 Debian 启动门和含 15 项资产的 Release 全部成功。 |
| v0.6.0 本机神秘站点中转 | 服务端 lint/integration/build、加密配置测试、客户端 lint/unit/UI/build、三套 E2E、真实回环服务与 `xbrowser` | 已通过并发布 | 服务端 21 项通过、2 项 PostgreSQL 条件跳过；主 E2E 56 项通过、6 项条件跳过，互联网 3 项、内网 2 项通过。覆盖错误密码与 429 限流、精确 Pages Origin、PNA header、三站点管理、Key 不回显、公开目录不含真实地址、模型发现、只发送确认脱敏材料、加密文件无明文、服务重启恢复和 revision 一键刷新。[Pages run 30333276540](https://github.com/NextWeb4/gw/actions/runs/30333276540) 与 [desktop run 30333304899](https://github.com/NextWeb4/gw/actions/runs/30333304899) 均成功；后续 Chrome LNA 权限诊断由 v0.6.1 补齐。 |
| v0.5.0 AI 历史、预制指引、拖拽导入与下载中心 | `pnpm lint`、`pnpm format:check`、`pnpm test`、`pnpm build`、三套 E2E、Windows 双版 x64 打包/启动、`xbrowser`、Pages/Release Actions | 已通过并发布 | 2026-07-27：主套件 56 通过、6 条件跳过，互联网/内网套件各 2 通过；覆盖 AI 历史搜索/重载/删除/快照、字段变化、默认无指引与三份预制、当前页精简 AI、拖拽 JSON、十二类分版/架构下载链接，以及互联网/内网空业务库。1440×900 实测三栏、折叠导航、AI 历史/指引和下载中心；390×844 实测 `body.scrollWidth=390`、主区底边 760、底栏顶边 770。互联网/内网 Web 与桌面资源均为 0 source map、0 条虚构样例业务文案命中。[Pages run 30278880756](https://github.com/NextWeb4/gw/actions/runs/30278880756) 成功，线上页脚显示 0.5.0；[desktop run 30278900461](https://github.com/NextWeb4/gw/actions/runs/30278900461) 的 verify、8 个分版打包、8 个 Debian 门和 release 全部成功。未新增依赖、许可证、数据库 schema、私有 API 或自动 AI 联网。 |
| v0.4.1 常用项、省级样例与当前页 AI | `pnpm lint`、`pnpm test`、`pnpm format:check`、`pnpm build`、三套 E2E、`xbrowser`、Pages Actions | 已通过并上线 Pages | 2026-07-27：主套件 53 通过、5 条件跳过，互联网/内网套件各 2 通过；新增回归覆盖单位/人员分区新增、重命名、删除、重载持久化和业务候选复用，以及写作/周报保持原导航并在当前页完成 AI 脱敏、确认和只读结果。真实浏览器在 1440×900 与 390×844 检查目录和 AI 面板无重叠；[Pages run 30261141656](https://github.com/NextWeb4/gw/actions/runs/30261141656) 成功后，线上文件台账实测显示 `闽政〔2026〕1号` 与 `福建省人民政府办公厅`，写作 AI 面板保持 `data-tab=writing`，页脚显示 0.4.1。未新增依赖、数据库 schema 或自动联网行为。 |
| v0.4.0 AI 助手模块与审计修复 | `pnpm lint`、`pnpm test`、`pnpm test:e2e`（三套）、`pnpm build` | 已通过本机回归 | 2026-07-26 本机回归全部通过：`pnpm lint`、`pnpm test`（全部包单测 + content/workflow/ui 契约）、`pnpm build`（三分发版 Web 产物均重建）、主套件 E2E 51 通过 5 条件跳过、内网 E2E 2 通过、互联网 E2E 2 通过。回归中修复：提示条同文案连续弹出被 React 相同 state 合并吞掉（改为带自增 key 的状态）、脱敏顺序缺陷（邮箱先于手机号 + 数字边界，防止 `手机号@域名` 泄露域名）、三处 E2E 定位器歧义（迁移页新增 .md/.txt 导入器与关于页“打开 AI 助手”引入）、主套件超时上调至 60 秒；并清理恒真 `enhancedLocalMode` 死分支与未引用的 `__PRIVATE_SERVICES__` 常量。原云端会话记录：新增“AI 助手”导航模块（预设含智谱 GLM）、“统计分析”确定性统计模块（domain 纯函数+CSS 图形）、任务“智能识别填单”本机文字识别、“公文写作指引库（Skill）”三边界 guidance（20,000 字符上限）、“周报模板”章节结构管理（默认输出经基线对比逐字节一致）、“配合单位分组”追加合并、“类目配色”确定性色板、`/v1`/`/v4` 版本号端点解析（Web/桌面 IPC/私有网关三处一致）、写作与周报“AI 润色”预填、本机时区默认日期、数量输入缓冲、新回归 E2E。domain 新函数已在云端以严格模式编译并通过功能断言。桌面 `node --test`（7 项）、`pnpm test:ui` 契约（2 项）与 HELP 文档契约断言已在云端通过；受网络限制，vitest/Playwright/构建需在本机重跑后方可标记通过 |
| 根目录、公开客户端、私有服务端 `AGENTS.md` | 检查目录结构、脚本、边界和风险规则 | 已完成 | [根工作区](../../AGENTS.md)、[客户端](../AGENTS.md) 和 [服务端](../../hxhwang-gw-server/AGENTS.md) |
| 客户端 lint / 单元测试 / format | `pnpm lint`、`pnpm test`、`pnpm format:check` | 已通过 | 2026-07-26 回归通过；覆盖四位年份与真实日期、常用项集合合并、六类台账、库存汇总、六类周报、公开/互联网/内网 AI、同步允许清单、迁移、文档、快照、Electron、来源策略、发布矩阵、详细说明书和 UI 契约 |
| 客户端构建 | `pnpm build`、`pnpm content:verify`、`pnpm assets:verify` | 已通过 | Web、领域包、迁移、文档、桌面壳构建通过；3 条规则、11 个模板、1 条授权记录及 Web/Electron 品牌资产校验通过 |
| 历史 HTML 原件 | 对根目录与 `legacy/` 执行 SHA-256 对比并核对真实存储键 | 已通过 | `升级版04.html` 与 `wenxibuddy0722.html` 均逐字节一致；0722 仍是需求基线，0723 与 UI glossary 只作补充参考；正式功能未写回历史原型。审计确认两版导出器都写入相同 `sourceApp/version` 且默认只收集 `work_/attach_`，因此迁移报告会披露来源歧义和缺失 Skill；物资 `attachments` 为记录内 Data URL，迁移器会另行生成稳定 ID、哈希和只读档案下载引用 |
| 历史 Skill / 配置 / 未映射字段 | 两版夹具、补充 Skill 输入、只读详情 E2E | 已通过 | 任务、文件和档案保留的 `legacyPayload` 可展开查看；普通配置与补充 Skill 进入独立只读列表，Skill 正文和额外原始字段均可见。所有内容经 React 文本节点/JSON 序列化显示，不使用 `dangerouslySetInnerHTML` |
| Pages 端到端与离线演示 | `pnpm test:e2e` | 已通过 | 2026-07-26 回归为 28 passed、4 条件跳过；覆盖显式操作前零外联、离线重开、Pages 私有同步隐藏、本机附件/业务导入、用户自备 Key AI、六类业务 CRUD、日期中间编辑状态、常用项保留、双版本迁移与复制边界、快照、DOCX/HTML/TXT、自定义格式、六类周报和移动布局 |
| v0.3.0 视觉系统 | `xbrowser` 真实浏览器、源码规则扫描、1440×900 与 390×844 截图 | 已通过本机验收 | 工作台、任务列表和写作中心采用原创“动态档案 / 墨迹信号”界面；功能图标全部来自 Lucide。390×844 实测正文区域为 `0..760`、底栏为 `770..834`、页面宽度为 390，页面级无横向溢出；1440×900 主区和侧栏完整分栏。控制台零错误，网络只包含同源 `/gw/` 资源和 `data:` 图像 |
| 周报闭环 | 领域测试、RxDB schema v2/快照、私有同步测试、Playwright 保存/重载/导出 | 已通过 | 新周报按日期范围从任务、会议、文件、外出、用章和物资已录入字段确定性汇总，可人工编辑、保存版本、恢复快照、显式同步并导出 A4 DOCX/PDF；旧版周报继续作为 `archive` 只读保留，版本号仍是覆盖保存计数而非可回退历史 |
| 内网 Web | `pnpm test:e2e:intranet`、`pnpm build:web:intranet`、`pnpm test:workflows` | 已通过本机构建与 E2E | 2026-07-26 的 2 项 E2E 通过；验证本机附件/迁移、私有 CSP、连接前零外联、认证会话、内部模型发现、可读结果、脱敏预览和确认发送；产物固定到 `dist-intranet/`，尚未部署到实际内网服务器 |
| 互联网 Web | `pnpm test:e2e:internet`、`pnpm build:web:internet` | 已通过本机构建与 E2E | 2026-07-26 的 1 项 E2E 通过；验证连接前零外联、会话级 API Key、以 `/v1` 结尾的地址、模型发现、可读结果、脱敏预览和显式确认；产物固定到 `dist-internet/`，浏览器实际接入仍取决于提供方 CORS |
| GitHub Pages 子路径 | `VITE_BASE_PATH=/gw/ pnpm --filter @hxhwang/web build` + 本地预览访问 `/gw/` | 已通过 | `http://127.0.0.1:4176/gw/` 返回 HTTP 200；入口 JS、PWA manifest 与品牌图标均使用 `/gw/` 前缀，真实浏览器未请求私有 API、模型服务或公共 CDN |
| 发布前仓库敏感信息扫描 | 对两个当前工作区执行 PAT、模型密钥、API key 和私钥模式扫描 | 已通过当前源码扫描 | 客户端与私有服务端当前源码均无对话中暴露的 PAT/模型令牌、硬编码 API key 或私钥；生成安装包和构建产物不纳入 Git 提交。历史扫描结论仍以既有发布记录为准 |
| Electron `file://` 资源 | `pnpm build:desktop:win:x64`、`pnpm build:desktop:win:arm64`；`pnpm verify:desktop-web` | 已通过 | 打包前校验 9 个资源均为相对路径，并验证桌面 CSP 仅允许 HTTPS、本机 API且禁止表单提交；ASAR 包含开发地址策略模块且不包含对应测试文件 |
| Windows 安装包 | Windows 本机 x64/arm64 electron-builder | 已通过构建 | x64 安装包 100,887,834 字节，SHA-256 `B39A27E1370ED82C6BB1FF11D4CA1086DF4104ABDF1593653A55B26769230360`；arm64 安装包 93,557,443 字节，SHA-256 `C014513CBB17E5C1D04D849BC8C1974068208F2E36D632B96C6ED8BCC276DC4B`；x64 解包程序启动后观察到 4 个进程并全部退出；arm64 未在本机启动 |
| Windows v0.1.1 本机构建 | `pnpm build:desktop:win:x64`、解包程序启动冒烟 | x64 已通过 | `HxHwang-Gw-0.1.1-x64-setup.exe` 为 100,944,547 字节，SHA-256 `E28CA7739CBC6B209BAD60FAD4F8B5AA7922F49DC93DF55940CF296AFB3F5803`；新解包程序启动后观察到 4 个进程并全部退出。正式多架构产物另由 Release Actions 验证并发布 |
| Windows v0.2.0 本机构建 | `pnpm build:desktop:win:x64`、解包程序启动冒烟 | x64 已通过 | `HxHwang-Gw-0.2.0-x64-setup.exe` 为 100,950,072 字节，SHA-256 `B88546A7874CAB44650E951DF1267E3F587A69C5B8C305764E4B5AE0D3432445`；解包程序启动后观察到 4 个进程并全部退出。正式多架构产物另由 Release Actions 验证并发布 |
| Windows v0.2.1 本机构建 | `pnpm build:desktop:win:x64`、解包程序启动冒烟 | x64 已通过 | `HxHwang-Gw-0.2.1-x64-setup.exe` 为 100,950,085 字节，SHA-256 `D80844FE1453F89E35218C789A766839CD82E6788C946FC7759D539B67D2827C`；解包程序启动后观察到 4 个进程并全部退出。正式多架构产物另由 Release Actions 验证并发布 |
| Windows v0.3.0 双版本本机构建 | `pnpm build:desktop:win:x64:internet`、`pnpm build:desktop:win:x64:intranet`、解包程序启动冒烟 | x64 已通过 | 互联网版安装包 101,310,147 字节，SHA-256 `102B3F644AF87CA35D31BC9E372A7984B2D4AFEAB761A16FF99C65298178AEAB`；内网版 101,310,153 字节，SHA-256 `B335D435668A9E4FF775B8C3C9F980434B160C472D681EBEAC0787EF95FD7BC7`。ASAR 元数据分别为 `internet`/`intranet`，两种解包程序均持续启动 8 秒；均未签名 |
| Windows v0.3.1 双版本本机构建 | 两条 x64 分版构建命令、解包程序 8 秒启动门、SHA-256 与 Authenticode 检查 | x64 已通过 | 互联网版安装包 101,310,186 字节，SHA-256 `77D9C0203B94E3A6FEB093FE4200B42B7CAB101A2F098CC2BA972B5DAF2D55DE`；内网版 101,310,243 字节，SHA-256 `28DB6AA0C422DD81454BB77B1112DD24A46132A8FA2F5BB79ECCEFBDCA08ABF3`。两版解包程序各保持 4 个进程运行 8 秒后由测试结束；互联网 ASAR 复核为 `hxhwangEdition=internet`，两版安装包均未签名。首次连续重打包复现临时 `EBUSY` 文件锁，现已用仅限 Windows/EBUSY/最多三次的有界 staging 重试修复并再次构建通过 |
| Windows v0.5.0 双版本本机构建 | 两条 x64 分版构建命令、解包程序 8 秒启动门、虚构样例扫描、SHA-256 与 Authenticode 检查 | x64 已通过 | 互联网版 `HxHwang-Gw-0.5.0-internet-x64-setup.exe` 为 99,966,947 字节，SHA-256 `84B61CD4EAA246DD2C9AB3D54A35A062AD9FE078EC9D95AC6C622713FBD92A90`；内网版为 99,966,968 字节，SHA-256 `280635F62EC2FA354B1FA5FE3344C5B88C5A1ED8C4F962C8F6940EDE331D7559`。两版解包程序各保持 4 个进程运行 8 秒；资源目录均为 0 source map、0 条虚构样例业务文案命中；两版安装包均为 `NotSigned`。 |
| Windows v0.6.0 双版本本机构建 | 两条 x64 分版构建命令、解包程序 10 秒启动门、敏感值扫描、SHA-256 与 Authenticode 检查 | x64 已通过 | 互联网版 `HxHwang-Gw-0.6.0-internet-x64-setup.exe` 为 99,968,574 字节，SHA-256 `6C4BF536C8308D789B552711E61A37A04DC9B9CA4710F6E91E3E794FA71ED2AC`；内网版为 99,968,492 字节，SHA-256 `8E1A9FAB72201710225614008C50BCA29753F9B7E7DEB0D927A1A7EAA13DE1FA`。两版解包主进程均持续运行 10 秒；源码、Web 产物与本机加密配置明文扫描通过；两版安装包均为 `NotSigned`。 |
| Windows 签名与品牌图标 | 资产结构校验、可执行文件图标提取、`Get-AuthenticodeSignature` | 图标已完成，签名未完成 | 原创 SVG 已生成 192/512 PNG 与 256 PNG-ICO，x64 可执行文件成功提取品牌图标；x64/arm64 安装包仍为 `NotSigned`，正式分发前需要代码签名证书 |
| 本机 Web 预览 | `/gw/` Web build、Playwright 与 `xbrowser` 可视化检查 | 已通过 | v0.3.0 带哈希的 JS/CSS、manifest 和品牌图标均可加载；1440×900 工作台/任务/写作中心和 390×844 写作中心无页面级横向溢出或面板重叠；验证服务器仅绑定 `127.0.0.1:4176` |
| 浏览器安全策略 | 检查 Pages/内网/桌面 CSP、Electron 开发地址策略并监听浏览器控制台 | 已通过并记录托管限制 | Pages/互联网版允许用户显式 HTTPS 或回环 AI 地址，但加载页面、选择预设和填写 Key 均零外联；Pages 不包含私有 API 目标。Electron 阻止任意新窗口、导航、webview 和非剪贴板权限，内网桌面主进程拒绝直连公网 AI；正式防嵌入仍须由 Web 响应头提供 |
| 私有同步与 AI | 单元测试、公开/互联网/内网 E2E、服务端集成测试 | 已通过演示契约 | 内网同步显式覆盖任务、会议、文件、外出、用章、物资、主草稿、周报和附件；内网 AI 只经认证网关。公开 Pages/互联网版支持服务商预设、会话级 Key、模型发现、五类本机素材与自定义材料；各模式都要求本地脱敏和逐次确认，结果只读且不覆盖原稿或确定性规则 |
| 授权资料元数据 | `pnpm content:verify` | 结构已完成，凭证编号待补 | 已记录来源文件、版本、授权范围、版权归属状态与限制；当前依据用户确认，正式公开前仍应补入授权书归档位置或凭证编号 |
| 公共权威与授权来源同步 | `pnpm test:content`、`pnpm content:sync`、`pnpm content:verify`；`.github/workflows/content-sync.yml` | 本地已通过，远端待运行 | 政府来源必须使用明确列出的 HTTPS 主机，其他来源必须精确匹配显式允许自动抓取的授权 URL；逐跳校验重定向并限制为 HTML、2 MB 和必含结构。2026-07-24 实际检查 1 个国家标准来源且哈希未变化；每周工作流仅在变化时提交生成元数据并在同一工作流部署 Pages，不改人工模板 |
| 服务端静态检查、集成测试、构建 | `pnpm lint`、`pnpm test:integration`、`pnpm build`、`pnpm format:check` | 已通过 | 2026-07-30 回归为 34 项通过、2 项 PostgreSQL 环境测试跳过；覆盖认证模型列表、默认模型、provider key 隔离、双模型路径、双鉴权、脱敏 relay 诊断、停用 provider 管理测试、revision 不变、会话、同步冲突、附件哈希、未确认 AI、重定向阻断与 CORS；构建验证生产入口。真实 PostgreSQL 状态单独见下一行，不以跳过项代替。 |
| 真实 PostgreSQL | `DATABASE_URL=... pnpm test:postgres`；CI 提供 PostgreSQL 16 服务 | 远端 CI 已通过，本机未执行 | 私有服务端 Actions run `30090037370` 在 PostgreSQL 16 服务上执行迁移、同步 checkpoint、冲突保留和附件往返测试并成功；缺少 `DATABASE_URL` 仍会快速失败，不能把内存 Store 测试当作真实数据库验证 |
| Debian 10/12 amd64/arm64 | `.github/workflows/desktop.yml` 八个分版 `.deb` 安装与 xvfb 启动门 | Actions 八组合已通过 | v0.3.1 run [`30165346085`](https://github.com/NextWeb4/gw/actions/runs/30165346085) 构建 Windows/Linux、x64/arm64、internet/intranet 八类 artifact，并在 Debian 10/12 的 amd64/arm64 容器中仅通过各自 DEB 元数据安装运行库后持续启动 20 秒。八个门全部成功；仅在 CI 容器启动参数使用 `--no-sandbox`，生产客户端未改变；QEMU 验证不能替代真实 arm64 设备测试 |
| GitHub Release | `v*` 标签触发桌面矩阵、Debian 门禁、校验清单和发布任务 | v0.7.8 已正式发布 | [Release v0.7.8](https://github.com/NextWeb4/gw/releases/tag/v0.7.8) 已发布；[run 30685866201](https://github.com/NextWeb4/gw/actions/runs/30685866201) 成功。Release 包含十二个互联网/内网、Windows/Linux、x64/arm64 安装文件和 `SHA256SUMS.txt`；13 个直接下载地址均返回 HTTP 200 且非空，校验文件含 12 条格式有效记录，SHA-256 与文件名分别唯一，文件名与安装资产完全一致。 |
| 外部商业与设计参考站 | 浏览器审查首页、可发现条款/隐私链接和 robots | 已完成 | 公文宝与 MotionSites AI 仅记录在 `docs/REFERENCE_AUDIT.md`；均未加入 `content/sources/allowlist.yaml`，未复制页面、提示词、模板、代码或视觉资产。MotionSites 首页未链接条款或隐私政策，常见路径返回站内 404，按未授权处理；robots 允许访问不等于允许再分发 |
| GitHub Pages 正式部署 | GitHub Actions / Pages 实际发布 | v0.7.8 已上线 | [Pages run 30685858239](https://github.com/NextWeb4/gw/actions/runs/30685858239) 的 build 与 deploy 均成功。[线上站点](https://nextweb4.github.io/gw/) 返回 HTTP 200，功能部署时 `Last-Modified` 为 2026-08-01 05:32:13Z，并加载已验证的内容哈希入口 bundle；真实 Chromium 在 1440×900 与 390×844 下显示页脚 `0.7.8`，会议→任务 MRU 和当前 active 描述正确，最近项分别约 56.6/62px。两视口无动作请求、控制台错误、页面错误或横向溢出；移动面板底边约 747px，固定底栏顶边 770px。截图为 `test-results/recent-records-0.7.8/recent-records-live-desktop.png` 与 `test-results/recent-records-0.7.8/recent-records-live-mobile.png`。 |
